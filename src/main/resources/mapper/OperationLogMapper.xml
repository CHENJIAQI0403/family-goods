<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.familygoods.mapper.OperationLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO operation_log(item_id, item_name, operation_type, detail, operator)
        VALUES(#{itemId}, #{itemName}, #{operationType}, #{detail}, #{operator})
    </insert>

    <select id="selectPage" resultType="com.example.familygoods.entity.OperationLog">
        SELECT id, item_id, item_name, operation_type, detail, operator, create_time
        FROM operation_log
        ORDER BY id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(1) FROM operation_log
    </select>

</mapper>
