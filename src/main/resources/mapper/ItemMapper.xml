<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.familygoods.mapper.ItemMapper">

    <resultMap id="itemMap" type="com.example.familygoods.entity.GoodsItem">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="category_id" property="categoryId"/>
        <result column="quantity" property="quantity"/>
        <result column="low_threshold" property="lowThreshold"/>
        <result column="use_count" property="useCount"/>
        <result column="location" property="location"/>
        <result column="purchase_time" property="purchaseTime"/>
        <result column="expire_time" property="expireTime"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="category_name" property="categoryName"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO goods_item(name, category_id, quantity, low_threshold, location, purchase_time, expire_time, remark)
        VALUES(#{name}, #{categoryId}, #{quantity}, #{lowThreshold}, #{location}, #{purchaseTime}, #{expireTime}, #{remark})
    </insert>

    <update id="update">
        UPDATE goods_item
        SET name = #{name},
            category_id = #{categoryId},
            quantity = #{quantity},
            low_threshold = #{lowThreshold},
            location = #{location},
            purchase_time = #{purchaseTime},
            expire_time = #{expireTime},
            remark = #{remark}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM goods_item WHERE id = #{id}
    </delete>

    <delete id="batchDelete">
        DELETE FROM goods_item WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectById" resultMap="itemMap">
        SELECT i.*, c.name AS category_name
        FROM goods_item i
        LEFT JOIN goods_category c ON i.category_id = c.id
        WHERE i.id = #{id}
    </select>

    <sql id="queryCondition">
        <where>
            <if test="name != null and name != ''">
                AND i.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="categoryId != null">
                AND i.category_id = #{categoryId}
            </if>
            <if test="location != null and location != ''">
                AND i.location LIKE CONCAT('%', #{location}, '%')
            </if>
        </where>
    </sql>

    <select id="selectPageList" resultMap="itemMap">
        SELECT i.*, c.name AS category_name
        FROM goods_item i
        LEFT JOIN goods_category c ON i.category_id = c.id
        <include refid="queryCondition"/>
        ORDER BY i.id DESC
        LIMIT #{pageSize} OFFSET ${(pageNum - 1) * pageSize}
    </select>

    <select id="selectPageCount" resultType="long">
        SELECT COUNT(1) FROM goods_item i
        <include refid="queryCondition"/>
    </select>

    <select id="selectWarnList" resultMap="itemMap">
        SELECT i.*, c.name AS category_name
        FROM goods_item i
        LEFT JOIN goods_category c ON i.category_id = c.id
        WHERE i.expire_time IS NOT NULL
          AND i.expire_time &lt;= #{warnDate}
        ORDER BY i.expire_time ASC
    </select>

    <select id="countTotal" resultType="long">
        SELECT COUNT(1) FROM goods_item
    </select>

    <select id="countExpired" resultType="long">
        SELECT COUNT(1) FROM goods_item
        WHERE expire_time IS NOT NULL AND expire_time &lt; #{today}
    </select>

    <select id="countWarn" resultType="long">
        SELECT COUNT(1) FROM goods_item
        WHERE expire_time IS NOT NULL
          AND expire_time &gt;= #{today}
          AND expire_time &lt;= #{warnDate}
    </select>

    <select id="countByCategory" resultType="map">
        SELECT c.name AS categoryName, COUNT(i.id) AS count
        FROM goods_category c
        LEFT JOIN goods_item i ON c.id = i.category_id
        GROUP BY c.id, c.name
        ORDER BY c.id ASC
    </select>

    <select id="selectLowStockList" resultMap="itemMap">
        SELECT i.*, c.name AS category_name
        FROM goods_item i
        LEFT JOIN goods_category c ON i.category_id = c.id
        WHERE i.quantity &lt;= i.low_threshold
        ORDER BY i.quantity ASC
    </select>

    <select id="selectPurchaseList" resultMap="itemMap">
        SELECT i.*, c.name AS category_name
        FROM goods_item i
        LEFT JOIN goods_category c ON i.category_id = c.id
        WHERE (i.expire_time IS NOT NULL AND i.expire_time &lt; #{today})
           OR i.quantity &lt;= i.low_threshold
        ORDER BY i.expire_time IS NULL, i.expire_time ASC, i.quantity ASC
    </select>

    <select id="countLowStock" resultType="long">
        SELECT COUNT(1) FROM goods_item WHERE quantity &lt;= low_threshold
    </select>

    <select id="selectUseFrequencyTop" resultType="map">
        SELECT id, name, use_count AS useCount
        FROM goods_item
        ORDER BY use_count DESC
        LIMIT #{limit}
    </select>

    <update id="incrementUseCount">
        UPDATE goods_item SET use_count = use_count + 1 WHERE id = #{id}
    </update>

    <select id="selectAllForExport" resultMap="itemMap">
        SELECT i.*, c.name AS category_name
        FROM goods_item i
        LEFT JOIN goods_category c ON i.category_id = c.id
        ORDER BY i.id ASC
    </select>

</mapper>
